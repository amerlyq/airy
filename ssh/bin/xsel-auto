#!/bin/sh -eu
h=$(head -c 1; echo .)
h=${h%.}
{ printf '%s' "$h"; cat
} | if expr "${#h}" >/dev/null
then # echo yes >&2
  exec xsel  --input --clipboard "$@" >/dev/null
else # echo no >&2
  exec xsel --output --clipboard "$@" </dev/null
fi
