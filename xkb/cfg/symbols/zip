// vim:ft=cpp
// WKRND: for Qt6/libxkbcommon to access layouts w/o "-rule airy"

default partial alphanumeric_keys modifier_keys
xkb_symbols "zip" {
    // include "pc"
    include "airy(zip)"
    include "airy(mixin)"
};
