#!/bin/bash -eu

aur_inst opencode-bin
# exe=/opt/ai/bin/opencode
# url="https://github.com/anomalyco/${exe##*/}/releases/download/v1.1.53/${exe##*/}-linux-x64.tar.gz"
# dl="$PWD/${exe##*/}.tar.gz"
# [[ -s $dl ]] || curl --fail --silent --show-error --location --output "$dl" -- "$url"
# sha256sum "$dl"
# sha256sum --check <<< "2c922540fb8e0a4cd7e15ae4b6eb7bc1f69dbe7683d45ac7056059ee832e2ad3  $dl"
# tar -C "${exe%/*}" -xzvf "$dl"
# chmod +x "$exe"
# sudo ln -sT "$exe" /usr/local/bin/"${exe##*/}"
# opencode --version
