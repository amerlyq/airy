#!/bin/bash -e
# tags: net
source ~/.shell/func.d/cfgsetup

### LINKS ###
acc=$(dcache)/main
chmod 700 ~/.cache/gcalcli

if cfgOpt r; then
  # Initiate OAuth2 to generate ~/.gcalcli_oauth
  gcalcli list
  # Secondary acc
  # gcalcli --configFolder ~/work list
fi
