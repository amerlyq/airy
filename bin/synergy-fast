#!/bin/bash

source ~/.bash_export
if [ $? -ne 0 ]; then exit 1; fi

if [ "$1" == "s" ]; then
    synergys -f -c ~/.config/.synergy.conf
fi

if [ "$1" == "c" ]; then
    synergyc -f -c ~/.config/.synergy.conf $LOCAL_SIP
fi
