#!/bin/sh

ranger_shell() {
    # tempfile='/tmp/ranger_shelldir'
    # $SHELL
    # test -f "$tempfile" &&
    # if [ "$(cat -- "$tempfile")" != "$(echo -n `pwd`)" ]; then
    #     export RANGER_SHELL_CD="$(cat "$tempfile")"
    # fi
    #rm -f -- "$tempfile" > /dev/null
}

ranger_shell

