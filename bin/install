#!/bin/bash -e
source ~/.shell/func.d/packages

if distro arch; then
  pkg_inst bbe      # bin sed
  # DISABLED:(unhelpful): use my re-elf tools instead
  # http://xyne.archlinux.ca/projects/hexgrep/
  # aur_inst hexgrep  # bin grep & diff
fi
