#!/bin/bash

# http://www.ex.ua/filelist/$id.urls

if [ "${1:0:4}" == "http" ]; then
    wget -c -i $1 --trust-server-names=on --restrict-file-names=nocontrol
else
    cat "$1" | while read line; do
        wget -c -i --content-disposition $line
    done
fi
