#!/bin/bash -e
cd $(dirname $(readlink -m ${0})) && source ~/.shell/func.d/cfgsetup
[[ "$THEME" =~ ^(dark|light)$ ]] || THEME=dark

### LINKS ###
link "$PWD" ~/.shell
link "$PWD/input/dircolors-${THEME}.ansi" ~/.dircolors
link "$PWD/bash/bashrc" ~/.bashrc
link "$PWD/input/inputrc" ~/.inputrc
