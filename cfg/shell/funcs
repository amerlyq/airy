# vim: ft=sh

DIR=~/.shell/func.d
FLES=(system links git write)

if [[ ! -e "$DIR" ]]; then
    DIR=./cfg/bash/functions.d
fi

if [[ -d "$DIR" ]]; then
    for fl in ${FLES[@]}; do
        source "$DIR/$fl"
    done
else printf "Not exist '$DIR'"; fi
