# vim: ft=gitconfig
[git-up "fetch"]
    all = true
    prune = true

[git-up "rebase"]
;     arguments = --preserve-merges
    auto = true
    log-hook = git -c color.ui=always --no-pager diff --stat $1..$2
