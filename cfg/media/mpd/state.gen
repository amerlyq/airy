#!/bin/bash -e

dst=~/.mpd/state

opt() { if grep -q "^$1:" "$dst"
then sed -i "/^\($1: \).*/s//\1$2/" "$dst"
else echo "$1: $2" >> "$dst"
fi; }

opt random    0
opt repeat    1
opt single    0
opt consume   0
opt crossfade 0
