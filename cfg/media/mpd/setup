#!/bin/bash
cd $(dirname $(readlink -m ${0})) || exit

### LINKS ###
link(){ [[ ! -e $2 || -L $2 ]] && ln -sfvT "$1" $2; }

dir=~/.mpd && mkdir -p $dir

case "$CURR_PROF" in
home) link "$PWD/mpd-own.conf" "$dir/mpd.conf" ;;
   *) link "$PWD/mpd-sir.conf" "$dir/mpd.conf" ;;
esac
