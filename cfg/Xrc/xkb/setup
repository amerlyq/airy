#!/bin/bash -e
source ~/.shell/func.d/cfgsetup

### GENS ###
if cfgOpt s; then
    case "$ARGS"
    in elona) over="elona"
    ;; hum) over="hummer"
    ;; esac
    echo "Layout: $ARGS"
fi

"$PWD/xkbm.gen" $over
"$PWD/run"
