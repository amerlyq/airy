[Unit]
Description=Xorg Keyboard Modifiers - %P
Requires=wm.target
After=wm.target
PartOf=main.target
Before=main.target

[Service]
ExecStart=/bin/sh -c '/usr/bin/xcape -d >/dev/null'
Restart=always
RestartSec=1

[Install]
WantedBy=main.target
