[Unit]
Description=Daemon kbdd
Requires=wm.target
After=wm.target
Before=main.target
PartOf=main.target

[Service]
# Type=dbus
# BusName=ru.gentoo.KbddService
ExecStart=/usr/bin/kbdd --nodaemon
# User=%i
# Group=users
# Restart=on-failure
# Restart=always
# RestartSec=5

[Install]
# Alias=dbus-ru.gentoo.KbddService.service
WantedBy=main.target
