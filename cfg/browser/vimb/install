#!/bin/bash
# vim: ft=sh
source ~/.shell/func.d/packages || exit

apt_noprompt "libsoup2.4-dev libwebkit-dev libgtk-3-dev libwebkitgtk-3.0-dev"
# all of them -- to build vimb
git_install 'fanglingsu/vimb'

apt_noprompt "inotify-tools"
# inotify-tools | watch on downloaded files in dir and auto-open them
