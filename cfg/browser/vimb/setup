#!/bin/bash -e
THEME_ALLOWED='dark|light'
source ~/.shell/func.d/cfgsetup

### LINKS ###
dir=~/.config/vimb
# Skip if you only toggle them
cfgOpt t || link -t "$dir" config 'dld-start' theme setup
link "$dir/theme/${THEME}.css" "$dir/style.css"
