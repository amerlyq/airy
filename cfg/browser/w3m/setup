#!/bin/bash -e
cd $(dirname $(readlink -m ${0}))

### LINKS ###
link(){ [[ ! -e $2 || -L $2 ]] && ln -sfvT "$1" $2; }

dir=~/.w3m && mkdir -p $dir
link "$PWD/config" "$dir/config"
link "$PWD/keymap" "$dir/keymap"
