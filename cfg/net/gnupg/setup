#!/bin/bash -e
source ~/.shell/func.d/cfgsetup

### LINKS ###
mkdir -p ~/.gnupg && chmod 700 ~/.gnupg
link -t  ~/.gnupg gpg{,-agent}.conf

# svc_activate -cue gpg-agent

echo RELOADAGENT | tee >(gpg-connect-agent)
