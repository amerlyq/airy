#!/bin/bash -e
source ~/.shell/func.d/packages

pkg_inst "gnupg gnupg-agent"

if distro arch; then
    echo pkg_inst "keychain pineentry"

elif distro ubuntu; then
    pkg_inst "keychain pinentry-curses  pinentry-gtk2"
fi
