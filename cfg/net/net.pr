#!/bin/bash
# vim: ft=sh
source ~/.shell/func.d/packages
if [ $? -eq 1 ]; then exit 1; fi
# Net-tech untils

# nmap    | Network exploration tool and security / port scanner
# uget  | download manager, symbiont for flashgot
pkg_inst iw nmap network-manager uget

## FTP search
# lftp user@ftp.domain.net:/> find . | grep WORD
# OR mount the ftp folder locally using curlftpfs.

# iw phy
# sudo iw dev wlp3s0 scan
# sudo iwlist wlp3s0 scan | sed -n '/(Channel/p'
# iwlist wlp3s0 freq
