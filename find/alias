# vim:ft=sh

f()  { find . -type f -iname "*$@*"; }
fd() { find . -type d -iname "*$@*"; }
ff() { find . -type f -regex $@; }
# fl() { locate ${1?<what?>} | sed "/\(\/[^/]\+\)\{${2:-5}\}/d" | sort -u; }
# alias ff='find ./ | grep -i '
