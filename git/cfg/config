# vim: ft=gitconfig
# ATT: copy instead of symlink -- for "git config --add" to work locally

[include]
    path = cfg/aliases
    path = cfg/base
    path = cfg/colors
    path = cfg/diff
    path = cfg/ft
    path = cfg/git-up
