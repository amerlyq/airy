[project]
name = "transient-diff"
version = "0.1.0"
description = "Find feature-branch additions that disappeared before the tip"
requires-python = ">=3.12"

[project.scripts]
transient-diff = "transient_diff:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
