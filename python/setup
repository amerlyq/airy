#!/bin/bash -e
# tags: lang
source ~/.shell/func.d/cfgsetup

### LINKS ###
linkcp {,~/.}pdbrc
# DFL: generate by $ ipython profile create
linkcp -t ~/.ipython/profile_default 'ipython_config.py'
# pudb.cfg
# sudo chown -R $USER:$USER ~/.config/pudb

# DEBUG:INFO: locations for local system-wide packages
#   USAGE: echo /path/to/kirie/modules > ~/.local/lib/python3.9/site-packages/kirie.pth
python -m site
