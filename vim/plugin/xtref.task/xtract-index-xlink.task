DEV: insert cross-link when using :Xtract ※}!:nu

BUT: if selected text (or only first line) already has *anchor*
  -- then parse it and link to it, instead of creating new one

[_] THINK:MAYBE? make each link unique
  -- stack multiple unique anchors pointing to only one referal each
