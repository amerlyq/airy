IDEA: use xtref to refer to any sent/received message
  => maybe not entirely <unique>, but with small enough found set to traverse and pick by context

VIZ
  workmail
  weechat

WARN: don't gen tags(1) for messages -- they will be too much
  BET: always run free search through list of database providers
    * xapian/notmuch for workmail
    * weechat plugin for chat history
