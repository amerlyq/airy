#!/bin/bash
# vim: ft=sh
source ~/.bash/functions.d/packages
if [ $? -eq 1 ]; then exit 1; fi
# Net-tech untils

LST="$LST openssh-server openssh-client autossh rtorrent"
# openssh | server for ssh connections (Win: freesshd)
# autossh | autoconnecting client for permanent tunnels

apt_noprompt $LST
