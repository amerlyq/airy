#!/bin/bash
# vim: ft=sh
source ~/.bash/functions.d/packages
if [ $? -eq 1 ]; then exit 1; fi
# (2-6MB) SDL2, SDL libs

# apt-cache search libsdl2
# ./autogen.sh && ./configure && make && sudo make install

LST="$LST libsdl2-dev libsdl2-image-dev libsdl2-ttf-dev"
LST="$LST libsdl2-gfx-dev libsdl2-mixer-dev libsdl2-net-dev"

apt_noprompt "$LST"


# More recent versions and more libs altogether
# apt_noprompt mercurial
# hg_install 'http://hg.libsdl.org/SDL'
# hg_install 'http://hg.libsdl.org/SDL_image'

