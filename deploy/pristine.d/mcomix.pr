#!/bin/bash
# vim: ft=sh
source ~/.bash/functions.d/packages
if [ $? -eq 1 ]; then exit 1; fi
# MComix

git_install 'Oddegamra/mcomix'
# mcomix    # fork of comix, one of several apps in linux instead of mmce

