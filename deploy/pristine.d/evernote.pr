#!/bin/bash
# vim: ft=sh
source ~/.bash/functions.d/packages
if [ $? -eq 1 ]; then exit 1; fi
# Evernote

apt_noprompt python-setuptools
git_install 'VitaliyRodnenko/geeknote'
chmod u+rw,g-wx,o-rwx "$HOME/.python-eggs"
geeknote settings --editor vim
geeknote login

