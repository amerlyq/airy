#!/bin/bash
# vim: ft=sh
source ~/.bash/functions.d/packages
if [ $? -eq 1 ]; then exit 1; fi
# DEVELOPMENT

LST="$LST git tig git-core git-doc meld doxygen" #subversion
LST="$LST git-gui gitk" # GUI: extract them only for work

# tig # git cli interactive (instead of gitk) http://jonas.nitro.dk/tig/manual.html

# Those libs also necessary to build x32 under x64 system
LST="$LST cmake build-essential gcc-multilib g++-multilib lib32stdc++6 "
LST="$LST make pkg-config autoconf automake" #binutils-gold
# libc6-dbg:i386    # need to prevent valgrind error with strlen -> strnlen
LST="$LST cppcheck valgrind libc6-dbg:i386 gdb"
#LST="$LST rabbit valkyrie"  #GUI#

LST="$LST google-perftools libgoogle-perftools-dev libgoogle-glog-dev google-mock"
# Not to dld, but use already! Dld manually if there is new version or absent.

apt_noprompt $LST
