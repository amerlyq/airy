#!/bin/sh
exec /usr/bin/env -i -- \
  PATH="/usr/local/bin:/usr/local/sbin:/usr/bin:/bin" \
  SHELL=/bin/bash SHLVL=1 TERM="$TERM" \
  LANG="${LANG:-en_US.UTF-8}" \
  USER="$USER" HOME="$HOME" \
  "$@"
