<?xml version="1.0"?>
<!DOCTYPE fontconfig SYSTEM "fonts.dtd">
<fontconfig>

  <!-- Hinting and subpixel rendering, antialiasing -->
  <match target="font">
    <edit mode="assign" name="antialias"><bool>true</bool></edit>

    <!-- EXPL: enforce scalable fonts at all sizes, sacrificing rendering quality for small size -->
    <!-- <edit mode="assign" name="embeddedbitmap"><bool>false</bool></edit> -->

    <!-- EXPL: use it only for fonts with ugly or w/o hinting at all -->
    <!-- BUT conflicts with sub-pixel-rendering -->
    <!-- <edit mode="assign" name="autohint"><bool>false</bool></edit> -->

    <edit mode="assign" name="hinting"><bool>true</bool></edit>
    <!-- <edit mode="assign" name="hintstyle"><const>hintslight</const></edit> -->
    <!-- <edit mode="assign" name="lcdfilter"><const>lcddefault</const></edit> -->
    <!-- <edit mode="assign" name="rgba"><const>rgb</const></edit> -->
  </match>

  <!-- dpi -->
  <!-- <match target="pattern"> -->
  <!--   <edit name="dpi" mode="assign"><double>283</double></edit> -->
  <!-- </match> -->

</fontconfig>
<!-- vim: set ts=2 sw=2 sts=2: -->
